# Server
PORT=5000

# PostgreSQL (used by auth code - src/config/db.js)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=keetaby

# PostgreSQL (used by Prisma - games & categories)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/keetaby"

# Auth
JWT_SECRET=change_me

# Email (optional - used by src/services/mailService.js)
EMAIL_USER=
EMAIL_PASS=
